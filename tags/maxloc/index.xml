<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Ivan Pribec</title>
    <link>http://localhost:1313/tags/maxloc/</link>
    <description>Recent content on Ivan Pribec</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>© 2025 — Ivan Pribec — All rights reserved.</copyright>
    <lastBuildDate>Sat, 18 Jul 2026 00:00:00 +0200</lastBuildDate>
    <atom:link href="http://localhost:1313/tags/maxloc/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>What Does MAXLOC Return for an Empty Array?</title>
      <link>http://localhost:1313/posts/2026/07/what-does-maxloc-return-for-an-empty-array/</link>
      <pubDate>Sat, 18 Jul 2026 00:00:00 +0200</pubDate>
      <guid>http://localhost:1313/posts/2026/07/what-does-maxloc-return-for-an-empty-array/</guid>
      <description>&lt;p&gt;While iterating on an LU factorization routine in Fortran the other day, I stumbled into a failing test:&lt;/p&gt;






&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt; 5/12 Test  #5: find_pivot .......................***Failed    0.06 sec
testing find_pivot_maxloc
4&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;4&lt;/code&gt; on the second line is the code from an &lt;code&gt;error stop&lt;/code&gt;, and it came from this innocent-looking case:&lt;/p&gt;






&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;&#34;&gt;&lt;code class=&#34;language-fortran&#34; data-lang=&#34;fortran&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#9ca0b0;font-style:italic&#34;&gt;! Empty array
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#9ca0b0;font-style:italic&#34;&gt;&lt;/span&gt;x &lt;span style=&#34;color:#04a5e5;font-weight:bold&#34;&gt;=&lt;/span&gt; [&lt;span style=&#34;color:#8839ef&#34;&gt;real&lt;/span&gt;(dp) &lt;span style=&#34;color:#d20f39&#34;&gt;::&lt;/span&gt;]
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;res &lt;span style=&#34;color:#04a5e5;font-weight:bold&#34;&gt;=&lt;/span&gt; find_pivot(x, size(x))
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#8839ef&#34;&gt;if&lt;/span&gt; (res &lt;span style=&#34;color:#04a5e5;font-weight:bold&#34;&gt;/=&lt;/span&gt; &lt;span style=&#34;color:#fe640b&#34;&gt;0&lt;/span&gt;) &lt;span style=&#34;color:#8839ef&#34;&gt;error stop&lt;/span&gt; &lt;span style=&#34;color:#fe640b&#34;&gt;4&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;
&lt;p&gt;An empty array is a natural edge case for a pivot search, so the test simply asserts that searching an empty array returns &lt;code&gt;0&lt;/code&gt;, our sentinel for &amp;ldquo;no pivot found.&amp;rdquo; The kernel under test was a one-liner built around the &lt;code&gt;maxloc&lt;/code&gt; intrinsic:&lt;/p&gt;</description>
      <content:encoded><![CDATA[<p>While iterating on an LU factorization routine in Fortran the other day, I stumbled into a failing test:</p>






<pre tabindex="0"><code> 5/12 Test  #5: find_pivot .......................***Failed    0.06 sec
testing find_pivot_maxloc
4</code></pre>
<p>The <code>4</code> on the second line is the code from an <code>error stop</code>, and it came from this innocent-looking case:</p>






<div class="highlight"><pre tabindex="0" style="color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-fortran" data-lang="fortran"><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic">! Empty array
</span></span></span><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic"></span>x <span style="color:#04a5e5;font-weight:bold">=</span> [<span style="color:#8839ef">real</span>(dp) <span style="color:#d20f39">::</span>]
</span></span><span style="display:flex;"><span>res <span style="color:#04a5e5;font-weight:bold">=</span> find_pivot(x, size(x))
</span></span><span style="display:flex;"><span><span style="color:#8839ef">if</span> (res <span style="color:#04a5e5;font-weight:bold">/=</span> <span style="color:#fe640b">0</span>) <span style="color:#8839ef">error stop</span> <span style="color:#fe640b">4</span></span></span></code></pre></div>
<p>An empty array is a natural edge case for a pivot search, so the test simply asserts that searching an empty array returns <code>0</code>, our sentinel for &ldquo;no pivot found.&rdquo; The kernel under test was a one-liner built around the <code>maxloc</code> intrinsic:</p>






<div class="highlight"><pre tabindex="0" style="color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-fortran" data-lang="fortran"><span style="display:flex;"><span><span style="color:#8839ef">pure</span> <span style="color:#8839ef">integer</span> <span style="color:#8839ef">function</span> find_pivot_maxloc(a, n) <span style="color:#8839ef">result</span>(ip)
</span></span><span style="display:flex;"><span>    <span style="color:#8839ef">integer</span>,  <span style="color:#8839ef">intent</span>(in) <span style="color:#d20f39">::</span> n
</span></span><span style="display:flex;"><span>    <span style="color:#8839ef">real</span>(dp), <span style="color:#8839ef">intent</span>(in) <span style="color:#d20f39">::</span> a(n)
</span></span><span style="display:flex;"><span>    ip <span style="color:#04a5e5;font-weight:bold">=</span> <span style="color:#04a5e5">maxloc</span>(<span style="color:#04a5e5">abs</span>(a), <span style="color:#04a5e5">dim</span><span style="color:#04a5e5;font-weight:bold">=</span><span style="color:#fe640b">1</span>)
</span></span><span style="display:flex;"><span><span style="color:#8839ef">end</span> <span style="color:#8839ef">function</span></span></span></code></pre></div>
<p>Finding the index of the entry with the largest magnitude is exactly the partial-pivoting step in Gaussian elimination. It is such a common operation that the reference BLAS ships a dedicated routine for it, <a href="https://github.com/Reference-LAPACK/lapack/blob/master/BLAS/SRC/idamax.f"><code>IDAMAX</code></a>, used throughout LAPACK&rsquo;s factorization routines. Hold that thought&mdash;we&rsquo;ll come back to <code>IDAMAX</code> shortly.</p>
<h2 id="reproducing-the-surprise">Reproducing the surprise</h2>
<p>One prompt later I had a minimal reproducer, which you can run for yourself in <a href="https://godbolt.org/z/aWf119h3a">Compiler Explorer</a>. Compiled with the Intel Fortran compiler (<code>ifx</code>) using its default settings, <code>maxloc(abs(a), dim=1)</code> on a zero-sized array returns <code>1</code> rather than <code>0</code>. That single off-by-one sentinel is enough to trip the assertion and abort the test.</p>
<p>At first glance this looks like a plain compiler bug. It isn&rsquo;t&mdash;it&rsquo;s a deliberate, decades-old default.</p>
<h2 id="why-does-this-happen">Why does this happen?</h2>
<p>A quick web search turned up a relevant <a href="https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287">Intel Fortran forum thread</a>. As Steve Lionel explains there:</p>
<blockquote>
<p>The issue is that earlier standards didn&rsquo;t specify what happens with zero-sized arrays, and adding checks for that slows code down. Intel has traditionally been reluctant to make changes that reduce performance, though it has happened (one no longer needs /assume:realloc_lhs, for example). Changing the default here would slow down everyone&rsquo;s MAXLOC/MINLOC, so there will be some resistance.</p>
</blockquote>
<p>Here is the history in a nutshell. Fortran 95 and earlier let <code>MAXLOC</code>/<code>MINLOC</code> return <code>1</code> for a zero-sized array (or when every element of a <code>MASK</code> is false), and that was the compiler&rsquo;s behavior. Fortran 2003 tightened this: if the array has size zero, every element of the result shall be zero. Intel added the standard-conforming behavior behind the <a href="https://www.intel.com/content/www/us/en/docs/fortran-compiler/developer-guide-reference/2025-0/maxloc.html"><code>-assume [no]old_maxminloc</code></a> switch, but kept the <em>old</em> behavior&mdash;returning <code>1</code>&mdash;as the default, because the extra check for the empty case costs a little performance in the hot path.</p>
<p>So <code>ifx</code> isn&rsquo;t so much wrong as old-fashioned by default. The <code>4</code> in my test output was the compiler faithfully reproducing 1990s semantics.</p>
<h2 id="fixing-it">Fixing it</h2>
<p>There are a few ways out, depending on how much you value portability versus a clean hot path.</p>
<p><strong>Flip the switch.</strong> The most direct fix is to ask the compiler for the modern semantics:</p>






<pre tabindex="0"><code>-assume noold_maxminloc</code></pre>
<p>The fix suggested in the forum thread is the broader <code>-standard-semantics</code> flag, which turns on <code>noold_maxminloc</code> along with a whole batch of other standard-conforming behaviors. That makes it something of a &ldquo;big hammer&rdquo;: it changes semantics in many areas at once, so it can have knock-on effects&mdash;including on performance&mdash;in places you might have preferred to leave alone. Note also that the diagnostic flag <code>-stand f18</code> does <em>not</em> change semantics; it only controls warnings.</p>
<p><strong>Guard it yourself.</strong> If you&rsquo;d rather not depend on a build flag, add your own empty check:</p>






<div class="highlight"><pre tabindex="0" style="color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-fortran" data-lang="fortran"><span style="display:flex;"><span><span style="color:#8839ef">pure</span> <span style="color:#8839ef">integer</span> <span style="color:#8839ef">function</span> find_pivot_maxloc(a, n) <span style="color:#8839ef">result</span>(ip)
</span></span><span style="display:flex;"><span>    <span style="color:#8839ef">integer</span>,  <span style="color:#8839ef">intent</span>(in) <span style="color:#d20f39">::</span> n
</span></span><span style="display:flex;"><span>    <span style="color:#8839ef">real</span>(dp), <span style="color:#8839ef">intent</span>(in) <span style="color:#d20f39">::</span> a(n)
</span></span><span style="display:flex;"><span>    ip <span style="color:#04a5e5;font-weight:bold">=</span> <span style="color:#fe640b">0</span>
</span></span><span style="display:flex;"><span>    <span style="color:#8839ef">if</span> (n <span style="color:#04a5e5;font-weight:bold">&lt;</span> <span style="color:#fe640b">1</span>) <span style="color:#8839ef">return</span>
</span></span><span style="display:flex;"><span>    ip <span style="color:#04a5e5;font-weight:bold">=</span> <span style="color:#04a5e5">maxloc</span>(<span style="color:#04a5e5">abs</span>(a), <span style="color:#04a5e5">dim</span><span style="color:#04a5e5;font-weight:bold">=</span><span style="color:#fe640b">1</span>)
</span></span><span style="display:flex;"><span><span style="color:#8839ef">end</span> <span style="color:#8839ef">function</span></span></span></code></pre></div>
<p>The downside is that on a standard-conforming compiler you now pay for the check twice: once in your guard, and once again inside <code>maxloc</code>.</p>
<p>If that redundancy bothers you, you can hide the guard behind the preprocessor:</p>






<div class="highlight"><pre tabindex="0" style="color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-fortran" data-lang="fortran"><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic">#ifdef BAD_COMPILER
</span></span></span><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic"></span>    <span style="color:#8839ef">if</span> (n <span style="color:#04a5e5;font-weight:bold">&lt;</span> <span style="color:#fe640b">1</span>) <span style="color:#8839ef">return</span>
</span></span><span style="display:flex;"><span><span style="color:#d20f39">#</span><span style="color:#8839ef">endif</span></span></span></code></pre></div>
<p>This avoids the double check&mdash;until the day someone builds with <code>-standard-semantics</code>, at which point <code>maxloc</code> guards the empty case too and we are back to checking twice. Papering over that would mean tying the macro to the exact compiler flags in play. Ugh.</p>
<p>Which brings us back to <code>IDAMAX</code>. If you peek at the reference BLAS implementation, the very first thing it does is exactly the guard we just reinvented:</p>






<div class="highlight"><pre tabindex="0" style="color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-fortran" data-lang="fortran"><span style="display:flex;"><span><span style="color:#8839ef">INTEGER</span> <span style="color:#8839ef">FUNCTION</span> IDAMAX(N,DX,INCX)
</span></span><span style="display:flex;"><span>    ...
</span></span><span style="display:flex;"><span>    IDAMAX <span style="color:#04a5e5;font-weight:bold">=</span> <span style="color:#fe640b">0</span>
</span></span><span style="display:flex;"><span>    <span style="color:#8839ef">IF</span> (N.LT.<span style="color:#fe640b">1</span> .<span style="color:#04a5e5">OR</span>. INCX.LE.<span style="color:#fe640b">0</span>) <span style="color:#8839ef">RETURN</span>
</span></span><span style="display:flex;"><span>    IDAMAX <span style="color:#04a5e5;font-weight:bold">=</span> <span style="color:#fe640b">1</span>
</span></span><span style="display:flex;"><span>    ...</span></span></code></pre></div>
<p>A routine that has been in service since the late 1970s already returns <code>0</code> for an empty input. The explicit check has been there all along; the modern intrinsic just tucked it behind a compiler switch.</p>
<h2 id="is-the-test-even-worth-it">Is the test even worth it?</h2>
<p>In practice, within the library we never actually call <code>maxloc</code> on an empty array&mdash;the pivot search always runs over a non-empty panel. By that logic the test is arguably pointless, and I could delete it and move on.</p>
<p>But &ldquo;we never do that&rdquo; has a way of expiring. The day someone copies this kernel into another project&mdash;where the empty case <em>can</em> occur&mdash;the old-array behavior is waiting to bite. A cheap test that pins down the contract for the edge case is good insurance, even if today&rsquo;s callers never exercise it.</p>
<h2 id="a-recommendation-for-new-projects">A recommendation for new projects</h2>
<p>For new work I have settled on flipping the switch at the build level and letting the intrinsic behave the way the standard says. In CMake, you can scope the flag to just the Intel compilers:</p>






<div class="highlight"><pre tabindex="0" style="color:#4c4f69;background-color:#eff1f5;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-cmake" data-lang="cmake"><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic"># Make ifx/ifort return 0 from MAXLOC/MINLOC on zero-sized arrays,
</span></span></span><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic"># matching the Fortran 2003+ standard.
</span></span></span><span style="display:flex;"><span><span style="color:#9ca0b0;font-style:italic"></span><span style="color:#04a5e5">add_compile_options</span>(
</span></span><span style="display:flex;"><span>  <span style="color:#40a02b">&#34;$&lt;$&lt;COMPILE_LANG_AND_ID:Fortran,Intel,IntelLLVM&gt;:-assume;noold_maxminloc&gt;&#34;</span>)</span></span></code></pre></div>
<p>If you would rather opt into the full set of modern semantics, swap <code>-assume;noold_maxminloc</code> for <code>-standard-semantics</code>&mdash;just be aware that it changes more than this one corner.</p>
<p>Either way, the lesson is an old one: intrinsics have contracts, those contracts have edges, and the edges are where the interesting bugs live.</p>
]]></content:encoded>
    </item>
    <item>
      <title>Zero-sized arrays and the maxloc/minloc conformity issue</title>
      <link>http://localhost:1313/posts/2025/11/zero-sized-arrays-and-the-maxloc/minloc-conformity-issue/</link>
      <pubDate>Mon, 24 Nov 2025 00:00:00 +0100</pubDate>
      <guid>http://localhost:1313/posts/2025/11/zero-sized-arrays-and-the-maxloc/minloc-conformity-issue/</guid>
      <description>&lt;p&gt;Reference links:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Intel forum thread: &lt;a href=&#34;https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287&#34;&gt;https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;GodBolt reproducer
&lt;a href=&#34;https://godbolt.org/z/aWf119h3a&#34;&gt;https://godbolt.org/z/aWf119h3a&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;In a recent iteration of LU factorization routine in Fortran, I stumbled into a failing test,&lt;/p&gt;






&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt; 5/12 Test  #5: find_pivot .......................***Failed    0.06 sec
testing find_pivot_maxloc
4&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The test which was failing was,&lt;/p&gt;






&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;        ! Empty array
        x = [real(dp) ::]
        res = find_pivot(x, size(x))
        if (res /= 0) error stop 4&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;and the kernel that was failing looked like this,&lt;/p&gt;






&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;    pure integer function find_pivot_maxloc(a, n) result(ip)
        integer,  intent(in) :: n
        real(dp), intent(in) :: a(n)
        ip = maxloc(abs(a), dim=1)
    end function&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In BLAS, they use the &lt;code&gt;IDAMAX&lt;/code&gt; function for this purpose.&lt;/p&gt;</description>
      <content:encoded><![CDATA[<p>Reference links:</p>
<ul>
<li>Intel forum thread: <a href="https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287">https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287</a></li>
</ul>
<p>GodBolt reproducer
<a href="https://godbolt.org/z/aWf119h3a">https://godbolt.org/z/aWf119h3a</a></p>
<p>In a recent iteration of LU factorization routine in Fortran, I stumbled into a failing test,</p>






<pre tabindex="0"><code> 5/12 Test  #5: find_pivot .......................***Failed    0.06 sec
testing find_pivot_maxloc
4</code></pre>
<p>The test which was failing was,</p>






<pre tabindex="0"><code>        ! Empty array
        x = [real(dp) ::]
        res = find_pivot(x, size(x))
        if (res /= 0) error stop 4</code></pre>
<p>and the kernel that was failing looked like this,</p>






<pre tabindex="0"><code>    pure integer function find_pivot_maxloc(a, n) result(ip)
        integer,  intent(in) :: n
        real(dp), intent(in) :: a(n)
        ip = maxloc(abs(a), dim=1)
    end function</code></pre>
<p>In BLAS, they use the <code>IDAMAX</code> function for this purpose.</p>
<p>One prompt later I had created a small reproducer.
You can check the failing test for yourself in Compiler Explorer: <a href="https://godbolt.org/z/aWf119h3a">https://godbolt.org/z/aWf119h3a</a></p>
<p>A quick Google search directed immediately directed me an Intel Fortran Forum thread from 2023: <a href="https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287">https://community.intel.com/t5/Intel-Fortran-Compiler/Incorrect-minloc-maxloc-results-with-ifx-2024-0/td-p/1549287</a></p>
<p>As Steve Lionel explains in that thread,</p>
<blockquote>
<p>The issue is that earlier standards didn&rsquo;t specify what happens with zero-sized arrays, and adding checks for that slows code down. Intel has traditionally been reluctant to make changes that reduce performance, though it has happened (one no longer needs /assume:realloc_lhs, for example).  Changing the default here would slow down everyone&rsquo;s MAXLOC/MINLOC, so there will be some resistance.</p>
</blockquote>
<p>The suggested fix for this issue, is adding the compiler flag <code>-standard-semantics</code>.
Note that the flags <code>-stand f18</code> doesn&rsquo;t change semantics and controls diagnostics only.
The issue with the <code>-standard-semantics</code> flag is it&rsquo;s a &ldquo;big hammer&rdquo; that influences many areas, so it can have an impact on performance in places that you may want to leave as is.</p>
<p>An easy fix for this issue is to just add our own empty check,</p>






<pre tabindex="0"><code>   ip = 0
   if (n &lt; 1) return
   ip = maxloc(abs(a),dim=1)</code></pre>
<p>but now we have the downside of performing two-checks with other compilers.</p>
<p>At the cost of having ugly code, we could guard the new statement like this,</p>






<pre tabindex="0"><code>#ifdef BAD_COMPILER
	if (n &lt; 1) return
#endif</code></pre>
<p>This avoid double-checking, unless circumstances lead us to use -standard-semantics in our build. To avoid that we&rsquo;d need a new guard depending on the compiler flags, ugh&hellip;</p>
<p>Now in practice, within the library, we in fact never call maxloc with an empty arrays, so perhaps the test is pointless to begin with.
But you never know when the day will arrive the function is copied in to a different project and the empty array behaviors comes to bite you.</p>
<p>For future projects consider adding the following settings to your CMake project or response file.</p>
<p>add_compile_options(
&ldquo;$&lt;$&lt;COMPILE_LANG_AND_ID:Fortran,Intel,IntelLLVM&gt;:-standard-semantics&gt;&rdquo;)</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
